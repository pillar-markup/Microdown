SmalltalkCISpec {
  #preLoading : '.github/scripts/preLoading.st',
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'Microdown',
      #onConflict : #useIncoming,
      #ignoreImage : true,
      #directory : 'src',
      #platforms : [ #pharo ]
    }
  ]
}
